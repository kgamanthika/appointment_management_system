DB_HOST=localhost
DB_USER=root   # For example: 'root' or 'your-username'
DB_PASSWORD=a2m3e1s8h  # For example: 'your-password'
DB_NAME=appointment_system     # Ensure this matches your DB name
JWT_SECRET=your-jwt-secret     # Your JWT secret for token authentication
EMAIL_USER=manthikaamesh@gmail.com  # Email for notifications
EMAIL_PASS=a2m3e1s8h  # Password for the email (or an app password)
